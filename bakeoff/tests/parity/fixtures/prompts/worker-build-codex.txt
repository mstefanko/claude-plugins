You are implementing a code change in an isolated git worktree. Your job:
1. Modify files in the current working tree to satisfy the requested goal.
2. Keep the patch scoped to the goal and background.
3. Add or update tests when they are useful evidence for the acceptance criteria.
4. Do not commit. The harness will capture your patch after you finish.

<question>
Document the prompt contract.
</question>

<context>
Stable prompt fixture context.
</context>

<scope>
Search the current working directory and cite as `path/to/file.ext:line`. Do not invoke web search.
</scope>

<build_spec>
Base ref: HEAD
Patch max bytes: 100000
Comparison goal: Prefer the clearer patch with stronger test coverage.

Verifier commands:
- unit (gate): go test ./...; timeout=300s; max_output=60000 bytes
- latency (metric): ./scripts/bench-json; timeout=300s; max_output=60000 bytes; metric=elapsed_ms direction=lower min_delta=10% noise_floor=5%
</build_spec>

<facet>
Facet id: code-review
Focus: Find actionable defects introduced or exposed by the change.

This is a task focus, not a persona. Do not role-play. Apply the facet only after the work-order goal, scope, citation rules, and output schema.

Include:
- correctness bugs and edge cases

Exclude:
- style-only preferences
</facet>

<rules>
- Prefer findings inside the facet.
- Do not invent domain facts to satisfy the facet.
- If you notice a severe issue outside the facet, place it in `recommended_next_checks` with a citation instead of expanding the main `claims` set.
- The facet never overrides output schema, citation requirements, or scope enforcement.
- Work only inside the current worktree. Do not modify files outside it.
- You are running under Codex workspace-write sandboxing; keep edits within the provided repository CWD and do not request broader filesystem access.
- Do not commit, create branches, or run commands that mutate the source checkout.
- Make the implementation production-quality and aligned with nearby code.
- Provider-authored tests or benchmarks are useful patch content, but the harness decides using the predeclared verifier commands.
- If you cannot implement the change, emit `status: "blocked"` and explain why in `summary` and `risks`.
</rules>

<runtime_budget>
The harness will stop this provider after 3 seconds.
Plan to stop investigation by about 1 seconds and reserve the
remaining time to emit a schema-valid <final_json>.

If full coverage is not possible before the cutoff:
- Prefer fewer well-cited findings over broad uncited coverage.
- Emit a partial but schema-valid result before the cutoff.
- Use existing uncertainty or rationale fields in the requested schema to record
  unfinished areas.
- Do not add fields outside the requested schema.
- Do not wait for perfect coverage if that risks missing the final_json cutoff.

Do not emit progress updates or partial JSON outside the final <final_json>
block. stdout is the structured answer channel.
</runtime_budget>

<worker_result_schema>
All build worker <final_json> payloads MUST include this top-level shape:

{
  "status": "complete",
  "summary": "What changed and why.",
  "files_touched": ["path/to/file.go"],
  "tests_added_or_changed": ["path/to/file_test.go"],
  "risks": [],
  "manual_checks": []
}

Allowed status values: "complete", "blocked".
Use `files_touched` for changed implementation, test, docs, scripts, and fixture paths.
Use `tests_added_or_changed` only for test-related files you added or modified.
Put unverifiable concerns in `risks` as strings.
Put commands you personally ran in `manual_checks` as strings.
Do not add fields outside the requested schema.
</worker_result_schema>

<process>
1. Inspect the relevant code and tests before editing.
2. Implement the smallest coherent patch that satisfies the goal and background.
3. Run focused checks when feasible.
4. Emit the JSON object per the worker result schema. No prose outside the JSON.
</process>

<output_format>
Reason in <scratchpad>...</scratchpad>, then emit one JSON object wrapped in <final_json>...</final_json>, matching the build worker result schema. No content after </final_json>.
</output_format>
