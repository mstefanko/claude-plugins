<final_json>
{"status":"complete","claims":[{"id":"R-001","claim":"researchRunDirIsIncomplete returns false when decision.json is present (treating the dir as complete), but no test covers this path. A run dir containing work-order.json plus decision.json must NOT be reclaimed without --force, yet the two new tests only exercise the reclaim-succeeds and unknown-files-block paths. A regression here would silently clobber completed runs.","evidence":["bakeoff/internal/commands/researchcmd/run.go:232-234 (returns false if decision.json exists)","bakeoff/internal/commands/researchcmd/run_test.go:116-170 (TestRunResearchReclaimsIncompleteRunDirWithoutForce, TestRunResearchDoesNotReclaimIncompleteRunDirWithUnknownFiles — neither asserts the completed-run rejection path)"],"severity":"high","confidence":"high"},{"id":"R-002","claim":"The new else-if branch `else if !os.IsNotExist(err) { return &apperror.RuntimeError{Err: err} }` in RunResearch has no test. This branch fires when os.Stat returns a non-not-found error (e.g. permission denied), which is a new error path introduced in this diff. It is the only new branch in the top-level RunResearch control flow that has no corresponding test case.","evidence":["bakeoff/internal/commands/researchcmd/run.go:78 (else if !os.IsNotExist(err) branch)","bakeoff/internal/commands/researchcmd/run_test.go:116-213 (no test exercises an os.Stat failure returning a non-IsNotExist error)"],"severity":"medium","confidence":"high"},{"id":"R-003","claim":"validateSingleProviderDecision's len(decision)==0 early-return branch is untested. When decision.json is missing or unreadable readOptionalObject returns an empty map, and validateSingleProviderDecision should return 'missing or unreadable decision.json'. TestRunValidatesSingleProviderDecisionSemantics provides a populated but semantically wrong decision — it never tests the absent-decision path.","evidence":["bakeoff/internal/verify/verify.go:353-355 (if len(decision) == 0 return specific error message)","bakeoff/internal/verify/verify_test.go:261-288 (TestRunValidatesSingleProviderDecisionSemantics uses populated decision with bad values, not absent decision)"],"severity":"medium","confidence":"high"},{"id":"R-004","claim":"TestRunProjectsExperimentFromManifest writes condition_id, run_kind, and repetition_index into the manifest fixture but only asserts id, task_id, slot_id, and slot_attempt in the result. ExperimentMap() populates all seven fields; if condition_id, run_kind, or repetition_index were accidentally dropped or renamed in the projection, this test would pass silently.","evidence":["bakeoff/internal/verify/verify_test.go:253 (result.Experiment assertion checks only id, task_id, slot_id, slot_attempt)","bakeoff/internal/verify/verify.go:222-236 (ExperimentMap populates condition_id, run_kind, repetition_index)"],"severity":"medium","confidence":"high"},{"id":"R-005","claim":"No test asserts that R-NNN is omitted from the glossary in a non-single-provider pairwise run when judge_ran is false. The production code gates R-NNN on jsonutil.BoolValue(decision['judge_ran']). TestRenderPairwiseGlossaryKeepsJudgeAndNonwinnerTerms passes judge_ran:true. A pairwise run where the judge stalled would get a stale R-NNN glossary entry.","evidence":["bakeoff/internal/report/report.go:1113-1117 (R-NNN added only if judge_ran is true)","bakeoff/internal/report/report_test.go:172-199 (both new tests: single-provider uses judge_ran:false, pairwise uses judge_ran:true — the combination pairwise+judge_ran:false is absent)"],"severity":"low","confidence":"high"},{"id":"R-006","claim":"diagnosticStderrOnly is tested only when artifact.ProviderSucceeded returns true (status:ok). No test covers the case where stderr_kind=='diagnostic' but the provider failed (status != ok) — in that case diagnosticStderrOnly should return false, leaving the truncation counted. If ProviderSucceeded's logic is wrong, or the status field key differs, the count could silently drop for failed providers.","evidence":["bakeoff/internal/manifest/manifest.go:676 (diagnosticStderrOnly returns true only when ProviderSucceeded also true)","bakeoff/internal/manifest/manifest_test.go:941-957 (only test: status field absent, stderr_kind:diagnostic — relies on ProviderSucceeded returning true for that shape; no test with status:failed+diagnostic)"],"severity":"medium","confidence":"medium"},{"id":"R-007","claim":"The 'analyze tiebreak basis' manifest telemetry test only provides spine_tiebreak (no selection_basis field). It verifies the fallback path of firstNonEmpty but not the primary: a decision that already has a non-empty selection_basis should use that value rather than falling back to spine_tiebreak. If the field-name order in firstNonEmpty were swapped, the primary path would silently use the fallback.","evidence":["bakeoff/internal/manifest/manifest.go:476 (firstNonEmpty(decision['selection_basis'], decision['spine_tiebreak']))","bakeoff/internal/manifest/manifest_test.go:616-634 (analyze tiebreak basis fixture has only spine_tiebreak, not selection_basis — exercises fallback only)"],"severity":"low","confidence":"high"},{"id":"R-008","claim":"No test covers the enforcement_level boundary: mechanisms non-empty AND fallbackReasons non-empty (should remain 'partial', not 'enforced'). The new OR condition in scope.go is `len(mechanisms) > 0 && len(fallbackReasons) == 0`. The only new assertion tests codex codebase (mechanisms present, fallbackReasons empty → enforced). The case with both non-empty is uncovered.","evidence":["bakeoff/internal/scope/scope.go:110 (new OR condition: enforced when mechanisms>0 && fallbackReasons==0)","bakeoff/internal/scope/scope_test.go:52-54 (only asserts enforced for codex-codebase which has no fallback reasons; no test for mechanisms>0+fallbackReasons>0 staying partial)"],"severity":"medium","confidence":"high"},{"id":"R-009","claim":"TestBuildResearchProjectsSingleProviderFields covers single_provider_result but not single_provider_failed. The summary package has at least two distinct single-provider outcomes; if BuildResearch fails to project RunMode or SingleProvider for the failed variant, no test would catch it.","evidence":["bakeoff/internal/summary/summary_test.go:68-80 (TestBuildResearchProjectsSingleProviderFields uses decision_kind:single_provider_result only)","bakeoff/internal/decision/decision.go (SingleProviderFailed is a distinct path referenced in plan context at verify.go:355 'single_provider_failed')"],"severity":"low","confidence":"medium"},{"id":"R-010","claim":"manifest.go addExperimentManifestFields precedence is untested: when both meta['experiment'] and workOrder['experiment'] are populated with different data, meta should win. The test TestWriteRunManifestFallsBackToWorkOrderExperiment only writes experiment in the work-order (not meta). No test confirms meta-present takes precedence over a work-order experiment, so a future refactor inverting the check order would pass all tests.","evidence":["bakeoff/internal/manifest/manifest.go:384-392 (tries meta first, falls back to workOrder)","bakeoff/internal/manifest/manifest_test.go:283-313 (TestWriteRunManifestFallsBackToWorkOrderExperiment: work-order has experiment, meta has none — only the fallback direction is tested)"],"severity":"low","confidence":"high"},{"id":"R-011","claim":"TestRunResearchReclaimsIncompleteRunDirWithoutForce does not assert that manifest.json contains correct provider output or decision data — only that the file exists. The fake claude binary emits HTML-entity-encoded final_json tags which the shell passes as literal text, not angle brackets. If the bakeoff parser requires literal angle-bracket tags, the fake provider response is unparseable and the manifest may be written with empty/fallback content. The test would still pass because it only calls fsutil.FileExists.","evidence":["bakeoff/internal/commands/researchcmd/run_test.go:130-134 (fake binary emits &lt;final_json&gt; not <final_json>)","bakeoff/internal/commands/researchcmd/run_test.go:166 (only asserts fsutil.FileExists(manifest.json))"],"severity":"medium","confidence":"medium"},{"id":"R-012","claim":"verify_test.go has no test for the nil/absent Experiment case: when a run's manifest has no experiment fields, result.Experiment should be nil. ExperimentMap returns nil when m.ExperimentID is nil, but no test exercises Run() on a non-experiment manifest and asserts result.Experiment == nil.","evidence":["bakeoff/internal/verify/verify.go:211-213 (ExperimentMap returns nil when ExperimentID is nil)","bakeoff/internal/verify/verify_test.go:225-260 (only TestRunProjectsExperimentFromManifest covers experiment presence; no counterpart for absence)"],"severity":"low","confidence":"high"},{"id":"R-013","claim":"repocontext_test.go updates TestValidateProsePathsSkipsSlashDelimitedProse to use 'meta/manifest/ls/summary' (4-part path) but does not test that a 3+-part path with a blocked first segment (e.g. 'internal/report/file.go') is still NOT treated as prose. The switch on parts[0] for blocked prefixes now applies to multi-part paths, but no test verifies a 3+-part path starting with 'internal' still triggers a warning.","evidence":["bakeoff/internal/repocontext/repocontext.go:313-318 (switch parts[0] still blocks 'internal' prefix for any N-part path)","bakeoff/internal/repocontext/repocontext_test.go:43-48 (only tests the pass-through direction for the updated 4-part prose string; no test for blocked 3+-part paths)"],"severity":"low","confidence":"medium"}],"conflicts":[],"unknowns":["Whether ExperimentMap is tested for the SET slot_id+slot_attempt path: TestExperimentMapIncludesNullableSlotFields covers only the unset case. An existing test for the set case may exist outside the diff but is not verifiable from the patch alone.","Whether RenderEscalation with a single_provider decision is tested anywhere outside the diff (D-NNN conditional in escalation path).","Whether the ProviderSucceeded function used in diagnosticStderrOnly considers all possible status field shapes or only 'ok' string — cannot confirm without reading artifact.go which is outside the diff."],"recommended_next_checks":["run.go:98-101: EnsureChildPath is now called inside the replaceRunDir block rather than the outer block. For the incomplete-reclaim path (no --force), confirm there is a test or existing coverage for a runDir that escapes the outDir when reclaiming without --force. Evidence: bakeoff/internal/commands/researchcmd/run.go:98-101","manifest.go outputTruncationCount: confirm the build-diagnostics path (buildDiagnosticsOutputTruncationCount) is unaffected by the diagnosticStderrOnly change — build runs write to diagnostics.json so the shortcut at line 686 returns before reaching the per-provider loop. If build runs reach the per-provider loop, the diagnostic exemption could affect build truncation counts. Evidence: bakeoff/internal/manifest/manifest.go:664-690"]}
}
</final_json>
