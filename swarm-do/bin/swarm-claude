#!/usr/bin/env bash
# swarm-claude — alias for `swarm-run --backend claude`.
# See the swarm-do plugin README.
set -euo pipefail

if [[ $# -gt 0 && "${1:-}" != -* ]]; then
  set -- --issue "$1" "${@:2}"
fi

exec "$(dirname "$0")/swarm-run" --backend claude "$@"
