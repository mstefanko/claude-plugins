# Claude overlay — agent-codex-review

This role is **Codex-only**. The runner refuses `--backend claude` for this
role. This file exists only to keep the `{shared,<backend>}.md` pair
symmetric across roles. If you are reading this via Claude, re-run with
`--backend codex` or use `swarm-gpt-review`.
