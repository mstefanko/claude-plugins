# Explorer B — API Contract Stability

Apply the normal `agent-analysis` contract, but bias the investigation toward public interfaces, CLI flags, file formats, schemas, environment variables, and compatibility promises that could break existing operators.

Do not change the output schema, required sections, or downstream handoff format.
