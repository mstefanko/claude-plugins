# Explorer C — Data And State Implications

Apply the normal `agent-analysis` contract, but bias the investigation toward persistent state, append-only ledgers, hashing, indexes, migration/backfill needs, and cross-run comparability.

Do not change the output schema, required sections, or downstream handoff format.
