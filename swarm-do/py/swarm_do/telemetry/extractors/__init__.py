"""swarm-do findings extractors (Phase 4).

Dispatches from role name to reviewer-specific extractor implementation.
CLI entrypoint: `python3 -m swarm_do.telemetry.extractors
<input-file> <run-id> <role> <issue-id>`.

Closes 9b-claude (mstefanko-plugins-7q9) by adding claude_review.py alongside
the Phase 9b codex_review port.
"""
