Here is a diff. Deglaze it.

diff --git a/src/orders/repo.ts b/src/orders/repo.ts
--- a/src/orders/repo.ts
+++ b/src/orders/repo.ts
@@ -12,9 +12,12 @@ export class OrderRepo {
   async findRecentForTenant(tenantId: string, limit: number): Promise<Order[]> {
-    const sql = `SELECT * FROM orders WHERE tenant_id = '${tenantId}' ORDER BY created_at DESC LIMIT ${limit}`;
-    const rows = await this.db.query(sql);
+    const rows = await this.db.query(
+      'SELECT * FROM orders WHERE tenant_id = $1 ORDER BY created_at DESC LIMIT $2',
+      [tenantId, limit],
+    );
     return rows.map(toOrder);
   }
 }
diff --git a/src/orders/repo.test.ts b/src/orders/repo.test.ts
--- a/src/orders/repo.test.ts
+++ b/src/orders/repo.test.ts
@@ -30,4 +30,16 @@ describe('OrderRepo.findRecentForTenant', () => {
+  it('passes tenant id and limit as bound parameters', async () => {
+    const db = { query: vi.fn().mockResolvedValue([]) };
+    const repo = new OrderRepo(db as any);
+    await repo.findRecentForTenant('t-1', 20);
+    expect(db.query).toHaveBeenCalledWith(expect.stringContaining('$1'), ['t-1', 20]);
+  });
+
+  it('does not interpolate a quote character into the SQL text', async () => {
+    const db = { query: vi.fn().mockResolvedValue([]) };
+    const repo = new OrderRepo(db as any);
+    await repo.findRecentForTenant("t' OR 1=1 --", 5);
+    expect(db.query.mock.calls[0][0]).not.toContain("OR 1=1");
+  });
 });
